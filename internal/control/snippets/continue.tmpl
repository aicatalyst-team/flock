# Continue.dev → Flock (OpenAI API)
# Add to ~/.continue/config.json under "models":

{
  "title": "Flock · {{.Model}}",
  "provider": "openai",
  "model": "{{.Model}}",
  "apiBase": "{{.BaseURL}}/v1",
  "apiKey": "{{.Token}}"
}
